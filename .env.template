# ⚠ 复制此文件为 .env 并填入你的 API Key
# .env 已加入 .gitignore，不会提交

# ── DeepSeek ──
DEEPSEEK_API_KEY=sk-placeholder-请修改
DEEPSEEK_BASE_URL=https://api.deepseek.com

# ── 阿里云百炼（ASR + TTS + Embedding） ──
ALIYUN_API_KEY=sk-placeholder-请修改
ALIYUN_BASE_URL=https://dashscope.aliyuncs.com/api/v1

# ── ASR（Phase 4） ──
ASR_MODEL=paraformer-realtime-v2

# ── TTS（Phase 2） ──
TTS_MODEL=cosyvoice-v2
TTS_VOICE=longxiaochun_v2

# ── Embedding（Phase 3） ──
EMBEDDING_MODEL=text-embedding-v3

# ── 本地路径 ──
VECTOR_STORE_PATH=./data/vector_store
XLSX_PATH=./data/source/zhubihua/朱比华知识库.xlsx
CURRENT_CHARACTER=sakuya

# ── 文本模型选择 ──
LLM_MODEL=deepseek-flash
